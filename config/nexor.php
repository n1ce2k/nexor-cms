<?php

use Nexor\Cms\Enums\PropertyType;

return [

    /*
    |--------------------------------------------------------------------------
    | Маршруты админки
    |--------------------------------------------------------------------------
    |
    | Префикс URL и middleware, под которыми монтируется панель управления.
    | Имена маршрутов всегда начинаются с `admin.`.
    |
    */

    'route' => [
        'prefix' => env('NEXOR_ROUTE_PREFIX', 'admin'),
        'middleware' => ['web'],
    ],

    /*
    |--------------------------------------------------------------------------
    | Модель пользователя
    |--------------------------------------------------------------------------
    |
    | Класс пользователя приложения. Должен использовать трейт
    | Nexor\Cms\Models\Concerns\HasRoles и реализовывать контракт
    | Nexor\Cms\Contracts\NexorUser.
    |
    */

    'user_model' => env('NEXOR_USER_MODEL', 'App\\Models\\User'),

    /*
    |--------------------------------------------------------------------------
    | Хранилище файлов
    |--------------------------------------------------------------------------
    |
    | Диск, на который складываются загрузки из админки, и папки внутри него.
    |
    */

    'storage' => [
        'disk' => env('NEXOR_DISK', 'public'),
        'directories' => [
            'avatars' => 'avatars',
            'iblocks' => 'iblocks',
            'sections' => 'sections',
            'elements' => 'elements',
            'settings' => 'settings',
            'properties' => 'properties',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Точки входа Vite
    |--------------------------------------------------------------------------
    |
    | Пути передаются в @vite() на страницах админки. По умолчанию собираются
    | прямо из пакета в vendor; переопредели, если стили и скрипты пересобраны
    | в самом приложении.
    |
    */

    'assets' => [
        'vendor/n1ce2k/nexor-cms/resources/css/admin.css',
        'vendor/n1ce2k/nexor-cms/resources/js/admin.js',
    ],

    /*
    |--------------------------------------------------------------------------
    | Vue-панель
    |--------------------------------------------------------------------------
    |
    | `path` — подпуть внутри префикса админки, по которому монтируется SPA.
    | `panel_assets` — точки входа Vite для панели.
    | `panel_extensions` — дополнительные бандлы приложения: они загружаются до
    | монтирования и могут зарегистрировать свои поля, страницы и пункты меню
    | через window.Nexor.
    |
    */

    'panel' => [
        'path' => env('NEXOR_PANEL_PATH', 'vue'),
    ],

    'panel_assets' => [
        'packages/nexor-cms/resources/css/admin.css',
        'packages/nexor-cms/resources/js/panel/main.js',
    ],

    'panel_extensions' => [],

    /*
    |--------------------------------------------------------------------------
    | Брендирование
    |--------------------------------------------------------------------------
    */

    'brand' => [
        'name' => env('NEXOR_BRAND', 'NEXOR'),
        'initial' => env('NEXOR_BRAND_INITIAL', 'N'),
    ],

    /*
    |--------------------------------------------------------------------------
    | Пагинация
    |--------------------------------------------------------------------------
    */

    'per_page' => [
        'default' => 20,
        'elements' => 25,
        'logs' => 50,
    ],

    /*
    |--------------------------------------------------------------------------
    | Типы свойств
    |--------------------------------------------------------------------------
    |
    | Типы, доступные при создании свойства инфоблока. Убери лишние, чтобы
    | сократить выпадающий список, или оставь null, чтобы включить все.
    |
    */

    'property_types' => null,

    /*
    |--------------------------------------------------------------------------
    | Значения по умолчанию
    |--------------------------------------------------------------------------
    */

    'defaults' => [
        'property_type' => PropertyType::String->value,
        'sort' => 500,
    ],

];
