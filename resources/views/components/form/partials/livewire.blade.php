{{--
    Форма из админки с «Отправкой без перезагрузки» — Livewire-компонент.
    Вёрстка самой формы — шаблон `<template>-livewire.blade.php`.
--}}

@livewire('nexor::feedback-form', $livewireParams, key($livewireKey))
